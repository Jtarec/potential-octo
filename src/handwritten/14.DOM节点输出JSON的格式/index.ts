
export function DOM2JSON(domtree) {
  // let obj = { name: "" };
  // obj.name = domtree.tagName;
  // obj.children = [];
  // domtree.childNodes.forEach((child) => obj.children?.push(DOM2JSON(child)));
  // return obj;
}
